<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" type="text/css" href="/styles/main.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@100&display=swap"
      rel="stylesheet"
    />
    <title>Age Predictor</title>
  </head>
  <body>
    <div class="predict">
      <% if (age === null) { %>
      <h1>Enter your name below:</h1>

      <form action="/" method="POST">
        <input
          class="form-control"
          type="text"
          name="nameInput"
          placeholder="First Name"
        />
        <br />

        <label for="countries">
          Select your country for the most accurate results:
        </label>
        <br />
        <div id="dropdown">
          <select
            name="country"
            id="countries"
            class="btn btn-light dropdown-toggle"
            type="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
            <option value="" disabled selected hidden>Choose a country</option>
            <% for (const countryCode in countries) { %>
            <option value="<%= countryCode %>">
              <%= countries[countryCode] %>
            </option>
            <% } %>
          </select>
          <br /><br />
        </div>

        <div id="predict">
          <button class="btn btn-light" type="submit">Predict Age</button>
        </div>
      </form>
      <% } else { %>
      <h1>
        <% if (age === 'N/A') { %> Age prediction is not available for the
        selected name and country. <% } else { %> Your Predicted Age is: <%= age
        %> <% } %>
      </h1>

      <form action="/" method="GET">
        <button type="submit" class="btn btn-light">Try Again</button>
      </form>
      <% } %>
    </div>
  </body>
</html>
